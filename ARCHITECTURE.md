# 技术博客架构指南

## 一、整体架构思路

```
┌─────────────────────────────────────────────────────────────┐
│                      内容层 (Content)                         │
│  Markdown/MDX 文章、frontmatter 元数据、资源图片               │
└─────────────────────────────────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────┐
│                      数据层 (Data)                            │
│  解析 MD、按 slug/标签/分类索引、生成列表与摘要                 │
└─────────────────────────────────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────┐
│                      路由与页面 (Pages)                       │
│  首页 / 文章页 / 分类·标签 / 关于 / 归档                       │
└─────────────────────────────────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────┐
│                      展示层 (UI)                              │
│  布局、排版、代码高亮、数学公式、主题、响应式                    │
└─────────────────────────────────────────────────────────────┘
```

---

## 二、技术选型建议

| 需求         | 推荐方案              | 说明 |
|--------------|-----------------------|------|
| 框架         | **Next.js** 或 **Astro** | Next 生态丰富、适合加后台；Astro 以内容为主、首屏更快 |
| 内容格式     | **MDX**               | Markdown + React 组件，便于嵌入图表、Demo |
| 样式         | **Tailwind CSS**      | 快速实现精美排版与主题切换 |
| 代码高亮     | **Shiki** 或 **Prism** | 技术博客必备，支持多语言 |
| 数学公式     | **KaTeX**             | 比 MathJax 更轻、渲染快 |
| 部署         | **Vercel** / **Netlify** | 连 Git 即自动部署，免费额度足够 |

---

## 三、目录结构建议

```
blog/
├── content/                 # 内容层：只放文章与资源
│   ├── posts/               # 文章（按日期或主题组织）
│   │   ├── 2025-03-17-hello-world.mdx
│   │   └── ...
│   └── assets/              # 文章引用的图片等
│
├── src/                     # 或 app/（若用 Next App Router）
│   ├── components/          # 展示层组件
│   │   ├── layout/          # 站点头尾、侧栏、导航
│   │   ├── post/            # 文章页专用：TOC、代码块、引用
│   │   └── ui/              # 按钮、卡片、标签等通用 UI
│   ├── lib/                 # 数据层逻辑
│   │   ├── posts.ts         # 读取、解析、排序、按 slug 查询
│   │   └── mdx.ts           # MDX 编译配置
│   ├── styles/              # 全局样式、主题变量
│   └── app/ 或 pages/       # 路由与页面
│       ├── page.tsx         # 首页
│       ├── post/[slug]/     # 文章详情
│       ├── tag/[tag]/       # 按标签筛选（可选）
│       └── about/           # 关于页
│
├── public/                  # 静态资源（favicon、OG 图等）
├── ARCHITECTURE.md          # 本架构说明
└── README.md
```

---

## 四、内容层设计（Frontmatter 约定）

每篇 `.mdx` 建议包含：

```yaml
---
title: 文章标题
description: 简短摘要（用于列表与 SEO）
date: 2025-03-17
tags: [JavaScript, 性能, 架构]
category: 前端
draft: false
---
```

- **title / description / date**：列表展示与 SEO
- **tags / category**：分类与筛选
- **draft**：开发时隐藏未完成文章

---

## 五、数据层职责

- **列出文章**：扫描 `content/posts`，解析 frontmatter，按 `date` 倒序。
- **单篇查询**：按 `slug`（通常由文件名或 frontmatter 的 `slug` 决定）返回一篇的 MDX 与元数据。
- **标签/分类**：从 frontmatter 聚合出所有 tag/category，再提供「某 tag 下的文章列表」接口。

可放在 `src/lib/posts.ts`，用 `fs` 读文件（构建时）或配合 `contentlayer`/`next-mdx-remote` 等库。

---

## 六、展示层要点（「精美」落地）

1. **排版**
   - 正文用衬线或优质无衬线字体（如 Noto Serif、Source Han Sans）。
   - 行宽约 65–75 字符，行高 1.6–1.8，段间距适中。

2. **代码**
   - 统一代码块样式（背景、边框、圆角），语法高亮主题与站点主题一致（如暗色博客配暗色代码块）。

3. **主题**
   - 用 CSS 变量管理颜色，一份变量实现亮/暗主题切换。

4. **动效**
   - 列表 hover、页面过渡可做轻微动效，避免花哨影响阅读。

5. **SEO 与分享**
   - 每页 `title`、`description`、OG 图；文章页用 frontmatter 的 `title`、`description` 生成 meta。

---

## 七、推荐实现顺序

1. 搭好框架（Next.js 或 Astro）+ 基础路由（首页、文章页）。
2. 实现内容层：MDX + frontmatter 约定，以及 `lib/posts.ts` 的读取与列表。
3. 做文章详情页布局（标题、日期、标签、正文、TOC）。
4. 接入代码高亮与数学公式。
5. 统一样式与主题（字体、间距、亮/暗）。
6. 按需加：标签页、分类页、RSS、搜索。

如果你告诉我更倾向 **Next.js** 还是 **Astro**，以及是否要 **亮/暗主题**、**中文为主**，我可以按这个架构帮你生成一版可运行的脚手架（含示例文章与基础样式）。
