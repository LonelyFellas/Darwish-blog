---
title: 欢迎来到我的技术博客
description: 第一篇示例文章，介绍博客的架构与写作方式。
date: 2025-03-17
tags: [Next.js, 博客, MDX]
category: 随笔
draft: false
---

这是用 **Next.js** 与 **MDX** 搭建的技术博客。文章写在 `content/posts` 下，用 frontmatter 写元数据。

## 代码示例

下面是一段 TypeScript 示例，展示代码高亮效果：

```ts
function greet(name: string): string {
  return `你好，${name}！`;
}
console.log(greet("读者"));
```

## 列表与引用

- 支持 **加粗**、*斜体*
- 有序列表同样可用
- 引用块如下：

> 技术博客的价值在于把思考沉淀下来，并与人分享。

继续在 `content/posts` 下新建 `.mdx` 文件即可发布新文章。祝写作愉快。
